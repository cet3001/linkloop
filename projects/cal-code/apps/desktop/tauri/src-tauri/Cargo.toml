[package]
name = "cal-code"
version = "0.1.0"
description = "Cal Code desktop shell"
authors = ["Cal Code"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0" }

[dependencies]
tauri = { version = "2.0.0" }

[features]
custom-protocol = ["tauri/custom-protocol"]
